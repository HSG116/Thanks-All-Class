// Teacher Messages Data
const teacherMessages = {
    hamza: {
        title: "رسالة إلى الأستاذ حمزة",
        message: `أستاذي الموقر حمزة

حولت الأرقام لمصدر للثقة والمعادلات لخطوات نحو الفهم والنجاح

كنت صبورًا متفهمًا وخلقت من التحدي متعة تعلم

لن ننسى دعمك وابتسامتك التي جعلت المسائل أسهل والتعليم امتع`
    },
    yazan: {
        title: "رسالة إلى الأستاذ يزن",
        message: `أستاذنا الفاضل يزن

كنت لنا مرشدًا في الإيمان قبل التعليم زرعت فينا الطمأنينة رغم الصعاب

كل آية شرحتها وكل حديث نقلته خلّد في قلوبنا معاني الإيمان والرحمة

جزاك الله خيرًا على كل كلمة نطقتها كانت نورًا في زمن الظلام`
    },
    islam: {
        title: "رسالة إلى مس إسلام",
        message: `أستاذتي الموقرة إسلام

في عالم أصبح إلكتروني كنت أنتِ بوابتنا للفهم والإبداع

قدرتك على تبسيط التكنولوجيا جعلتنا نشعر أننا أقرب للمستقبل

شكرًا لأنك اعطيتِ من قلبك ومنحتينا أدوات نكبر فيها ونحلم أكثر`
    },
    alya: {
        title: "رسالة إلى مس علياء",
        message: `مس علياء الغالية

شرحتِ ببساطة وصبرتِ بمحبة وكل تجربة علمتيها كانت درسًا في الحياة

أنتِ العلم الممزوج بالأمل ونحن ممتنون لكل كلمة تشجيع و لكل لحظة تعلمنا فيها على يديكِ.`
    },
    amir: {
        title: "رسالة إلى الأستاذ أمير",
        message: `أستاذي الفاضل أمير

علّمتنا أن اللغة ليست مجرد كلمات بل جسر يوصلنا لعالم أوسع وأمل أكبر

في أصعب الظروف كنت لنا مصدر نور وسند علمتنا بإصرار واحتويتنا بكلماتك

شكرًا لأنك كنت أحد أبطال الرحلة وجعلت المستقبل يبدو أكثر إشراقًا

بارك الله فيك علمًا وخلقًا`
    },
    mohammad: {
        title: "رسالة إلى الأستاذ محمد الرجوب",
        message: `أستاذي الفاضل محمد

كنت صوتًا للبلاغة وأيقونة للصبر علّمتنا لغتنا الأم بحب وإتقان رغم البعد والتحديات

كلماتك كانت كالبلسم والدروس منك تركت أثرًا في أعماقنا

لك كل الشكر والتقدير والامتنان وجعل الله تعليمك صدقة جارية في الدنيا والآخرة 🤍`
    },
    murad: {
        title: "رسالة إلى الأستاذ مراد",
        message: `أستاذنا المحترم مراد

انت مش بس علمتنا عن التاريخ والجغرافيا علمتنا عن القوة والمقاومة والصمود

كنت حاضرًا معنا بالقلب ودروسك كانت أكثر من مجرد معلومات كانت دروس وطنية محفورة بالوجدان

دمت قدوة وبارك الله فيك 🌍`
    }
};

// Administration Messages Data
const adminMessages = {
    administration: {
        title: "رسالة إلى طاقم الإدارة كامل",
        message: `إلى إدارة المدرسة الإلكترونية مدرسة 《 الخارجي اساسي عليا (( 23 )) 》

أنتم كنتم الهيكل الذي وقف فيه كل هذا البناء

أنتم "الصوت المجهول" الذي ظل ينسق ويرتب ويدعم

ما كنّا نعرف وجوهكم كلها لكننا شعرنا بوجودكم في كل تفاصيل يومنا

في الرسالة التي وصلت في الصف الذي فُتح وفي المحبة التي تجوّلت داخل النظام

أنتم لم تكونوا إداريين فقط

كنتم "نظامًا عاطفيًا" يحملنا حين فشلت الأنظمة الأخرى`
    },
    counselor: {
        title: "رسالة إلى المرشدة التربوية آمال أبو زر",
        message: `مس آمال

حضنك التربوي كان عزاءً لمن أنهكهم التعب النفسي وكلماتك كانت البلسم للقلوب

سمعتينا من غير أحكام وكنتِ نقطة التوازن لما اختلت مشاعرنا

شكرًا لأنك كنتِ الأمان حين غابت البيوت والسند لما تعبنا بصمت

كل حرف منك محفور بالامتنان والدعاء 🤍♥`
    }
};

// Student Messages Data
const studentMessages = {
    mira: {
        name: "ميرا خالد اليازجي",
        message: "كنا مجرد طلاب، لكن بفضلكم أصبحنا نعرف من نكون، وإلى أين نمضي. شكرًا من القلب لكل من علّم وصبر وابتسم رغم الألم 💖"
    },
    mohammad: {
        name: "محمد زياد كنعان",
        message: "أنتم لم تعلمونا فقط دروسًا، بل منحتمونا دفعة لنكمل الحياة بإيمان أكبر. أنتم بصمة لن تُنسى ✨"
    }
};

// DOM Elements
const splashScreen = document.getElementById('splash-screen');
const mainContent = document.getElementById('main-content');
const typewriterText = document.getElementById('typewriter-text');
const messageModal = document.getElementById('message-modal');
const studentModal = document.getElementById('student-modal');
const addMessageModal = document.getElementById('add-message-modal');
const confettiContainer = document.getElementById('confetti-container');

// Typewriter control
let currentTypewriterTimeout = null;

// Typewriter Effect
function typeWriter(text, element, speed = 50) {
    // Clear any existing animation
    if (currentTypewriterTimeout) {
        clearTimeout(currentTypewriterTimeout);
        currentTypewriterTimeout = null;
    }
    
    let i = 0;
    element.innerHTML = '';
    
    // Split text into lines
    const lines = text.split('\n');
    let currentLine = 0;
    
    function type() {
        if (currentLine < lines.length) {
            if (i < lines[currentLine].length) {
                // Add character by character in the current line
                element.innerHTML = lines.slice(0, currentLine).join('<br>') +
                    '<br>' + lines[currentLine].slice(0, i + 1);
                i++;
                currentTypewriterTimeout = setTimeout(type, speed);
            } else {
                // Move to next line
                i = 0;
                currentLine++;
                currentTypewriterTimeout = setTimeout(type, speed * 2); // Longer pause between lines
            }
        } else {
            currentTypewriterTimeout = null;
        }
    }
    
    // Set RTL direction and proper styling
    element.style.direction = 'rtl';
    element.style.textAlign = 'right';
    element.style.lineHeight = '2';
    element.style.whiteSpace = 'pre-wrap';
    element.style.fontFamily = 'Tajawal, Arial, sans-serif';
    
    type();
}

// Splash Screen Animation
function initSplashScreen() {
    const splashText = "لكل من علّم بصبر، وصنع الأمل في قلب الألم... شكرًا معلمينا 💖";
    
    setTimeout(() => {
        typeWriter(splashText, typewriterText, 80);
    }, 1000);
    
    setTimeout(() => {
        splashScreen.style.opacity = '0';
        splashScreen.style.transform = 'translateY(-100%)';
        
        setTimeout(() => {
            splashScreen.style.display = 'none';
            mainContent.classList.remove('hidden');
            mainContent.classList.add('show');
        }, 1000);
    }, 6000);
}

// Confetti Animation
function createConfetti() {
    const colors = ['#ff6b6b', '#4ecdc4', '#45b7d1', '#f9ca24', '#f0932b', '#eb4d4b', '#6c5ce7'];
    
    for (let i = 0; i < 50; i++) {
        const confetti = document.createElement('div');
        confetti.className = 'confetti';
        confetti.style.left = Math.random() * 100 + '%';
        confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
        confetti.style.animationDelay = Math.random() * 3 + 's';
        confettiContainer.appendChild(confetti);
        
        setTimeout(() => {
            confetti.remove();
        }, 4000);
    }
}

// Modal Functions
function openModal(modal) {
    modal.style.display = 'block';
    document.body.style.overflow = 'hidden';
}

function closeModal(modal) {
    // Clear any running typewriter animation
    if (currentTypewriterTimeout) {
        clearTimeout(currentTypewriterTimeout);
        currentTypewriterTimeout = null;
    }
    
    // Clear the message content immediately
    const modalMessage = modal.querySelector('#modal-message');
    if (modalMessage) {
        modalMessage.innerHTML = '';
    }
    
    modal.style.display = 'none';
    document.body.style.overflow = 'auto';
    
    // Remove any remaining confetti
    const confetti = document.querySelectorAll('.confetti');
    confetti.forEach(c => c.remove());
}

// Teacher Message Modal
function showTeacherMessage(teacherId) {
    const teacher = teacherMessages[teacherId];
    if (!teacher) return;
    
    const modalTitle = document.getElementById('modal-title');
    const modalMessage = document.getElementById('modal-message');
    
    // Clear previous content
    modalTitle.textContent = '';
    modalMessage.innerHTML = '';
    
    // Set new content
    modalTitle.textContent = teacher.title;
    
    openModal(messageModal);
    createConfetti();
    
    // Start typewriter effect after a short delay
    setTimeout(() => {
        typeWriter(teacher.message, modalMessage, 30);
    }, 500);
}

// Admin Message Modal
function showAdminMessage(adminId) {
    const admin = adminMessages[adminId];
    if (!admin) return;
    
    const modalTitle = document.getElementById('modal-title');
    const modalMessage = document.getElementById('modal-message');
    
    // Clear previous content
    modalTitle.textContent = '';
    modalMessage.innerHTML = '';
    
    // Set new content
    modalTitle.textContent = admin.title;
    
    openModal(messageModal);
    createConfetti();
    
    // Start typewriter effect after a short delay
    setTimeout(() => {
        typeWriter(admin.message, modalMessage, 30);
    }, 500);
}

// Student Note Modal
function showStudentNote(studentId) {
    const student = studentMessages[studentId];
    if (!student) return;
    
    const studentModalTitle = document.getElementById('student-modal-title');
    const studentModalMessage = document.getElementById('student-modal-message');
    
    // Clear previous content
    studentModalTitle.textContent = '';
    studentModalMessage.innerHTML = '';
    
    // Set new content
    studentModalTitle.textContent = `💌 رسالة من ${student.name}`;
    
    openModal(studentModal);
    createFloatingHearts();
    
    // Start typewriter effect after a short delay
    setTimeout(() => {
        typeWriter(student.message, studentModalMessage, 40);
    }, 500);
}

// Floating Hearts Effect
function createFloatingHearts() {
    const hearts = ['💖', '💕', '💗', '💝', '💘'];
    
    for (let i = 0; i < 15; i++) {
        const heart = document.createElement('div');
        heart.textContent = hearts[Math.floor(Math.random() * hearts.length)];
        heart.style.position = 'fixed';
        heart.style.left = Math.random() * 100 + '%';
        heart.style.top = '100%';
        heart.style.fontSize = '2rem';
        heart.style.zIndex = '1001';
        heart.style.pointerEvents = 'none';
        heart.style.animation = 'floatUp 4s ease-out forwards';
        
        document.body.appendChild(heart);
        
        setTimeout(() => {
            heart.remove();
        }, 4000);
    }
}

// Add floating hearts animation to CSS
const style = document.createElement('style');
style.textContent = `
    @keyframes floatUp {
        0% {
            transform: translateY(0) rotate(0deg);
            opacity: 1;
        }
        100% {
            transform: translateY(-100vh) rotate(360deg);
            opacity: 0;
        }
    }
`;
document.head.appendChild(style);

// Print Function
function printMessage() {
    const modalContent = document.querySelector('.message-paper').innerHTML;
    const printWindow = window.open('', '_blank');
    printWindow.document.write(`
        <html>
            <head>
                <title>رسالة شكر</title>
                <style>
                    body { font-family: 'Tajawal', Arial, sans-serif; direction: rtl; text-align: right; padding: 20px; }
                    .message-paper { background: #ffeaa7; padding: 30px; border-radius: 10px; }
                    h3 { color: #2c3e50; text-align: center; }
                    .modal-message { line-height: 1.8; color: #2c3e50; }
                </style>
            </head>
            <body>
                <div class="message-paper">${modalContent}</div>
            </body>
        </html>
    `);
    printWindow.document.close();
    printWindow.print();
}

// Copy Function
function copyMessage() {
    const messageText = document.getElementById('modal-message').textContent;
    const titleText = document.getElementById('modal-title').textContent;
    const fullText = `${titleText}\n\n${messageText}`;
    
    navigator.clipboard.writeText(fullText).then(() => {
        alert('تم نسخ الرسالة بنجاح! 📋');
    }).catch(() => {
        // Fallback for older browsers
        const textArea = document.createElement('textarea');
        textArea.value = fullText;
        document.body.appendChild(textArea);
        textArea.select();
        document.execCommand('copy');
        document.body.removeChild(textArea);
        alert('تم نسخ الرسالة بنجاح! 📋');
    });
}

// Custom Message Functions
function loadCustomMessages() {
    const customMessages = JSON.parse(localStorage.getItem('customMessages') || '[]');
    const customMessagesContainer = document.getElementById('custom-messages');
    
    customMessagesContainer.innerHTML = '';
    
    customMessages.forEach((message, index) => {
        const messageCard = document.createElement('div');
        messageCard.className = 'custom-message-card';
        messageCard.innerHTML = `
            <h4 class="custom-message-name">💌 ${message.name}</h4>
            <p class="custom-message-text">${message.message}</p>
        `;
        customMessagesContainer.appendChild(messageCard);
    });
}

function saveCustomMessage(name, message) {
    const customMessages = JSON.parse(localStorage.getItem('customMessages') || '[]');
    customMessages.push({ name, message, date: new Date().toISOString() });
    localStorage.setItem('customMessages', JSON.stringify(customMessages));
    loadCustomMessages();
}

// Event Listeners
document.addEventListener('DOMContentLoaded', function() {
    // Initialize splash screen
    initSplashScreen();
    
    // Load custom messages
    loadCustomMessages();
    
    // Teacher card clicks
    document.querySelectorAll('.teacher-card').forEach(card => {
        const readBtn = card.querySelector('.read-message-btn');
        readBtn.addEventListener('click', () => {
            const teacherId = card.dataset.teacher;
            showTeacherMessage(teacherId);
        });
    });
    
    // Admin card clicks
    document.querySelectorAll('.admin-card').forEach(card => {
        const readBtn = card.querySelector('.read-message-btn');
        readBtn.addEventListener('click', () => {
            const adminId = card.dataset.admin;
            showAdminMessage(adminId);
        });
    });
    
    // Student card clicks
    document.querySelectorAll('.student-card').forEach(card => {
        const noteBtn = card.querySelector('.student-note-btn');
        noteBtn.addEventListener('click', () => {
            const studentId = card.dataset.student;
            showStudentNote(studentId);
        });
    });
    
    // Modal close buttons
    document.querySelectorAll('.close-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            const modal = e.target.closest('.modal');
            closeModal(modal);
        });
    });
    
    // Modal background clicks
    document.querySelectorAll('.modal').forEach(modal => {
        modal.addEventListener('click', (e) => {
            if (e.target === modal) {
                closeModal(modal);
            }
        });
    });
    
    // Print button
    document.getElementById('print-btn').addEventListener('click', printMessage);
    
    // Copy button
    document.getElementById('copy-btn').addEventListener('click', copyMessage);
    
    // Add message button
    document.getElementById('add-message-btn').addEventListener('click', () => {
        openModal(addMessageModal);
    });
    
    // Message form submission
    document.getElementById('message-form').addEventListener('submit', (e) => {
        e.preventDefault();
        
        const teacherName = document.getElementById('student-name').value.trim();
        const message = document.getElementById('message-text').value.trim();
        
        if (teacherName && message) {
            // Create WhatsApp message
            const whatsappMessage = `مرحبا، أريد أن أرسل رسالة شكر للأستاذ/ة ${teacherName}:\n\n${message}\n\nمع التقدير والامتنان 💖`;
            const whatsappUrl = `https://api.whatsapp.com/send?phone=972592311460&text=${encodeURIComponent(whatsappMessage)}`;
            
            // Open WhatsApp
            window.open(whatsappUrl, '_blank');
            
            // Save message locally
            saveCustomMessage(teacherName, message);
            closeModal(addMessageModal);
            
            // Reset form
            document.getElementById('message-form').reset();
            
            // Show success message
            alert('تم إرسال رسالتك عبر واتساب! شكرًا لك 💖');
            
            // Create confetti
            createConfetti();
        }
    });
    
    // Contact form submission
    document.querySelector('.contact-form').addEventListener('submit', function(e) {
        e.preventDefault();
        const name = document.getElementById('name').value;
        const phone = document.getElementById('phone').value;
        const message = document.getElementById('message').value;

        const whatsappUrl = `https://api.whatsapp.com/send?phone=972592311460&text=مرحبا%20اسمي%20${encodeURIComponent(name)}%20رقم%20هاتفي%20${encodeURIComponent(phone)}%20أود%20أن%20أستفسر%20عن:%20${encodeURIComponent(message)}`;

        window.location.href = whatsappUrl;
        this.reset();

        alert('شكراً لتواصلكم معنا! سنقوم بالرد عليكم في أقرب وقت.');
    });
    
    // Keyboard navigation
    document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape') {
            document.querySelectorAll('.modal').forEach(modal => {
                if (modal.style.display === 'block') {
                    closeModal(modal);
                }
            });
        }
    });
});

// Smooth scrolling for internal links
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});

// Add scroll animations
function animateOnScroll() {
    const elements = document.querySelectorAll('.teacher-card, .admin-card, .student-card');
    
    const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.style.animation = 'slideInUp 0.8s ease-out forwards';
            }
        });
    }, {
        threshold: 0.1
    });
    
    elements.forEach(element => {
        observer.observe(element);
    });
}

// Initialize scroll animations
document.addEventListener('DOMContentLoaded', animateOnScroll);

// Add floating stars to background
function createFloatingStars() {
    const starsContainer = document.querySelector('.floating-stars');
    if (!starsContainer) return;
    
    for (let i = 0; i < 20; i++) {
        const star = document.createElement('div');
        star.textContent = '✨';
        star.style.position = 'absolute';
        star.style.left = Math.random() * 100 + '%';
        star.style.top = Math.random() * 100 + '%';
        star.style.fontSize = Math.random() * 1.5 + 0.5 + 'rem';
        star.style.animation = `float ${Math.random() * 3 + 4}s ease-in-out infinite`;
        star.style.animationDelay = Math.random() * 2 + 's';
        star.style.opacity = Math.random() * 0.8 + 0.2;
        starsContainer.appendChild(star);
    }
}

// Initialize floating stars
setTimeout(createFloatingStars, 2000);

// Add touch support for mobile
let touchStartY = 0;
let touchEndY = 0;

document.addEventListener('touchstart', e => {
    touchStartY = e.changedTouches[0].screenY;
});

document.addEventListener('touchend', e => {
    touchEndY = e.changedTouches[0].screenY;
    handleSwipe();
});

function handleSwipe() {
    const swipeThreshold = 50;
    const diff = touchStartY - touchEndY;
    
    if (Math.abs(diff) > swipeThreshold) {
        if (diff > 0) {
            // Swipe up - could trigger some action
        } else {
            // Swipe down - could trigger some action
        }
    }
}

// Performance optimization: Lazy load animations
function lazyLoadAnimations() {
    const cards = document.querySelectorAll('.teacher-card, .admin-card, .student-card');
    
    const cardObserver = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                entry.target.classList.add('animate');
                cardObserver.unobserve(entry.target);
            }
        });
    }, {
        rootMargin: '50px'
    });
    
    cards.forEach(card => {
        cardObserver.observe(card);
    });
}

// Initialize lazy loading
document.addEventListener('DOMContentLoaded', lazyLoadAnimations);